<Window x:Class="Accorda.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Accordatore per Chitarra" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu>
            <MenuItem Header="File">
                <MenuItem Header="Leggi Licenza" Click="LeggiLicenza_Click"/>
                <MenuItem Header="Chiudi" Click="Chiudi_Click"/>
            </MenuItem>
        </Menu>

        <TextBlock Text="Accordatore per Chitarra" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,0" Grid.Row="0"/>
        <ComboBox Name="InputDevices" HorizontalAlignment="Center" Width="250" Margin="0,20,0,0" SelectionChanged="InputDevices_SelectionChanged" Grid.Row="1"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Frequenza Rilevata" FontSize="16" Margin="0,0,5,0"/>
            <TextBlock Name="FrequenzaAttuale" FontSize="36" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>